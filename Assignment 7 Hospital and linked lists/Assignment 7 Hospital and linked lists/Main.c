

void fill(int** matrix) {
	int arr[2][2] = { {1,2},{3,4} };
	for (int i = 0; i < 2;i++)
		for (int j = 0; j < 2;j++)
			matrix[i][j] = arr[i][j];
}
	int main() {
		int arr[2][2];
		fill(arr); 
		f(2);
	}
	int f(int x) {
		return 0;
	}
